//import {reqLocations} from '../src/helpers/requests';
//import {actSearchFieldVal} from '../src/actions/search'
////import fakeResponses from '../__mocks__/fakeResponses'
//import {mockPlaces} from '../__mocks__/fakeResponses';
//import stores from '../src/stores'
////import notiActions from '../../src/actions/notifications'
//import axios from 'axios'
//import path from 'path'

//const sessionChg = attrChangerValue(session)

jest.mock('axios')


beforeEach(()=>{
  //actSearchFieldVal('locFromOptions',[])
  //actSearchFieldVal('locToOptions',[])
})

test('simple test', () => {
  expect(true)
  //return reqLocations('barc','From')
  //.then((res)=>{
  //  expect(Array.isArray(res.data))
  //  expect(res.data.length).toBeGreaterThan(0)
  //  expect(stores.search.locFromOptions.slice()).toEqual(mockPlaces)
  //})
});
